# ProgressBar

Progress of a running scan, with a percentage and an optional line saying what is happening now.

## Props
- `value` (required) and `max` (default 100).
- `label` (required): what is progressing; also the accessible name.
- `detail`: the current step, in `body-sm` ("Nuclei · 1,204 of 3,870 templates").
