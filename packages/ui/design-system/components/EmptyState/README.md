# EmptyState

What a list shows before it has content, with the one action that fills it.

## Props
- `title` (required): what is missing, as a statement ("No websites yet").
- `children`: one sentence on what to do.
- `icon`: an `IconName`, default `scan-search`.
- `action`: usually a primary `Button`.
